import { StyleSheet } from 'react-native';

export const pTextAlignStyles = StyleSheet.create({
    left: {
        textAlign: 'left'
    },
    right: {
        textAlign: 'right'
    },
    center: {
        textAlign: 'center'
    },
});
